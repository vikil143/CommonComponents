import {Dimensions} from 'react-native';
// import {widthToDp} from './helpers';

const {width, height} = Dimensions.get('screen');

export const SCREEN_WIDTH = width;
export const SCREEN_HEIGHT = height;

export const RUPEE_SYMBOL = '\u20A8';
